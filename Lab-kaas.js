
var GeleKaas =prompt ('Is de kaas geel?')
if (GeleKaas >= 'ja'){
	var Kaasmetgaten = prompt ('Zitten er gaten in?')
}
	if (Kaasmetgaten == 'ja'){
		var DureKaas = prompt('Is de kaas belachelijk duur?')
	}
	if (DureKaas == 'ja'){
		var WelkeKaas = prompt ('Welke kaas is het?')
	}
		if (WelkeKaas == 'Emmenthaler'){
		document.write ('Gefeliciteerd u heeft het goed!')
	}
	    else {
	    document.write ('Sorry, probeer het opnieuw!')


}
